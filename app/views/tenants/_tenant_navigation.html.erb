<nav class="navbar navbar-default navbar-fixed-top navbar-inverse" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">

      <a class="navbar-brand" href="<%= root_path %>">Home!</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="#">Listings</a></li>
        <li><a href="#">Contact</a></li>

      </ul>
      <form class="navbar-form navbar-left" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
      </form>
      <div class="nav navbar-nav navbar-right">

        <div id="ibs-example-navbar-collapse-1">
          <a class="navbar-text btn" role="button" href="<%= tenant_path(@tenant) %>" >
            <b>Settings/Profile</b></a>
            <% if tenant_logged_in? %>
            <b><%= link_to "Logout", logout_path, method: :post, class: "navbar-text btn", role: "button" %></b>

            <% else %>
            <a class="navbar-text btn" role="button" href="<%= login_path %>">
              <b>Login</b></a>
            <% end %>


        </div>


      </div>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
