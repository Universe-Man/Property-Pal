<!-- <%= f.collection_select :property_id, @admins, :id, :fullname %>

<%= f.collection_select :admin_id, @admins, :id, :fullname %>

<%= f.collection_select :admin_id, @admins, :id, :fullname %> -->
